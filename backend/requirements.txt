fastapi==0.115.0
uvicorn[standard]==0.32.0
python-dotenv==1.0.1
pydantic==2.9.2
youtube-transcript-api==1.2.4
yt-dlp==2026.7.4
requests==2.32.3
openai==2.37.0
anthropic==0.107.1
google-genai==2.8.0
opencc-python-reimplemented==0.1.7
trafilatura==2.1.0
markitdown[pdf]==0.1.6

# 可選：掃描型（無文字層）PDF 的本地 OCR。沒裝時 app 照常運作，掃描 PDF 會回
# 「需裝 OCR」提示而非靜默。要啟用：pip install rapidocr-onnxruntime PyMuPDF
# （onnxruntime 約 50MB；打包進 sidecar 需在 PyInstaller spec collect 其模型資料）
# rapidocr-onnxruntime==1.3.24
# PyMuPDF==1.24.10

# 可選：WhisperX 引擎（make_vs_take 規格 TAKE）——VAD 切片 + 字級對齊，吃長音檔。
# 沒裝時 asr_mode=whisperx 回「未安裝」提示；預設 local=whisper.cpp 照常運作。
# 安裝（CPU 機器；注意順序——whisperx 會 pin CUDA torch，要事後換回 CPU 版，
# 否則 .venv 會多 ~5.6G 的 nvidia/triton 死重，撐爆打包）：
#   pip install whisperx                       # 會先拉 CUDA torch（暫時）
#   pip install "torch==2.8.0+cpu" "torchaudio==2.8.0+cpu" "torchvision==0.23.0+cpu" \
#       --index-url https://download.pytorch.org/whl/cpu   # 三個都要換 CPU 版（torchvision 漏換會 ABI 衝突）
#   pip uninstall -y triton nvidia-cublas-cu12 nvidia-cuda-cupti-cu12 \
#       nvidia-cuda-nvrtc-cu12 nvidia-cuda-runtime-cu12 nvidia-cudnn-cu12 \
#       nvidia-cufft-cu12 nvidia-cufile-cu12 nvidia-curand-cu12 \
#       nvidia-cusolver-cu12 nvidia-cusparse-cu12 nvidia-cusparselt-cu12 \
#       nvidia-nccl-cu12 nvidia-nvjitlink-cu12 nvidia-nvtx-cu12   # 移孤立 CUDA 庫
# 結果 .venv 7.7G→約 2.1G。移掉 triton 後 pip 會警告 whisperx 需 triton——無害
# （triton 是 GPU kernel 編譯器，CPU 路徑用不到；親驗 transcribe+VAD+對齊照跑）。
# CPU 推論：base 60s 音檔 ~18s（模型快取後）。語者分離（pyannote）依規格「待驗需求」
# 未接，需 HF token。
# whisperx==3.8.6
