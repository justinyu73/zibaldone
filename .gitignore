# Secrets — never commit
.env
*.key
credentials.json

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Build artifacts (regenerated by build_desktop.sh)
backend/build/
backend/dist/
*.spec

# Local ML models (gated download, not source) — e.g. Silero VAD onnx for 即時擷取 Half-2
backend/models/
*.onnx
frontend/src-tauri/binaries/
frontend/src-tauri/target/

# Node / Vite
node_modules/
frontend/dist/

# OS / editor
.DS_Store
*.code-workspace
test-results/
frontend/test-results/

# graphify derived knowledge graph (rebuild: graphify update .)
graphify-out/

# Local vault runtime output (test/dev meeting notes + rollbacks) — not source
02_Sources/
.local-assets/
news_ops/.source_data_root
meeting_zh.mp3
nltk_data
rec-test
repos
setup-tailscale-ssh.sh
setup-wsl-sshd.sh
yt-distill-demo
yt-meeting-demo
yt_note_test_meeting.mp3
